<div class="container">

    {{!-- 
Whitespace Control
Important for URLs with whitespace, if req.query variables empty, this results in whitespace (%20%20%20...)
To avoid it set ~ in handlebars if statement:  ~#if ... ~/if 
--}}

    <div class="row">
        <div class="col s12">
            <div class="card blue-grey darken-1">
                <div class="card-content white-text">
                    <span class="card-title">{{comic.title}}</span>
                    <div class="card-image">
                        <img src='/images/pages/{{comic.imagePath}}' class="comic-show">
                    </div>
                    <p><strong>Price:</strong> {{comic.price}} €</p>
                    <p><strong>Description:</strong> {{{comic.description}}}</p>
                    <div class="card-action">
                        <a href="/shop?page={{page}}
            {{~#if line}}&line={{line}}{{~/if}}
            {{~#if search}}&search={{search}}{{~/if}}" class="waves-effect waves-light btn btn-small right">BACK</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>